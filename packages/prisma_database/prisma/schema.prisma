generator client{
    provider = "prisma-client-js"
} 
datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

model User {
    id String @id @default(uuid())
    usernme String @unique
    password String 
    email String @unique
    phone String @unique

}